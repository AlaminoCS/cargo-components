import { banners } from './bannerData';
import { IMAGES } from './images';
import { contactLinks, mainLinks } from './navigationData';

export const headerData = {
  contactLinks,
  mainLinks,
  logoSrc: IMAGES.logo,
  banners,
};
